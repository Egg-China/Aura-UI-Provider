[package]
name = "aura-ui-provider"
version = "0.1.0"
edition = "2021"
description = "Aura Launcher isolated native UI frontend speaking the aura.ui.v1 protocol"
license = "Apache-2.0"

[dependencies]
tauri = { version = "2", features = [] }
serde_json = "1"

[build-dependencies]
tauri-build = { version = "2", features = [] }
